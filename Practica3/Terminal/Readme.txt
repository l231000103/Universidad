Monitor serial

monitor_serial.png -> captura del monitor serie (Arduino IDE 2) ejecutando millis.ino: se ve el mensaje "Ximena la mas chambeadora" repetido cada vez que se enciende el LED rojo (cada 3000 ms). La versión con delay() no imprime nada en el monitor serie.
