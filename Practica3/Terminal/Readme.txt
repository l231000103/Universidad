Monitor serial

Solo la versión con millis() usa el monitor serie (9600 baudios): cada vez que el LED rojo se enciende, se envía el mensaje "Ximena la mas chambeadora". La versión con delay() no imprime nada en el monitor serie.
