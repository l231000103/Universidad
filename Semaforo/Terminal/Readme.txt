Monitor serial

Este programa no usa Serial.begin() ni Serial.print(), por lo que no imprime nada en el monitor serie. El comportamiento del semáforo se verifica de forma visual (LEDs) y con el botón.
